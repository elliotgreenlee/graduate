class Stack:

    def __init__(self):
        stack = []
        return

    def push(self, new_item):
        # add item to top of stack
        return

    def pop(self):

        return # top of stack


class Queue:

    def __init__(self):
        stack_first_top = []  # This stack is in first to last order from top down
        stack_last_top = []  # This stack is in last to first order from top down

    # add item to last in queue
    def enqueue(self, new_item):

        # while stack_first_top has items
            # pop item from stack_first_top
            # push item to stack_last_top

        # push new_item to stack_last_top
        return

    # get item from first in queue
    def dequeue(self):

        # while stack_last_top has items
            # pop item from stack_last_top
            # push item to stack_first_top

        # pop top of stack_first_top
        return